#include "main.h"

/**
  * mul - a functions that multiplies two integers
  * @a: first input
  * @b: second input
  * Return: Results of the two inputs
  */
int mul(int a, int b)
{
	return (a * b);
}
